Drop the orc / enemy character model here.

- Prefer .glb (or .gltf + .bin + textures). Avoid .fbx.
- Must be RIGGED + ANIMATED (idle/walk/attack/death) so I can drive it from the orc AI.
- After dropping files in, tell me and I'll import + wire them up.
